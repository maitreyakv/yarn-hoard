services:
  api:
    build: 
      context: ./backend
      target: base
    ports:
      - "3000:3000"
    command: "/bin/sh -c 'cargo run'"
    develop:
      watch:
        - action: sync+restart
          path: ./backend
          target: /app
          ignore: 
          - target
